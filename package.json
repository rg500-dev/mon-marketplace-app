{
  "name": "marketplace",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install-frontend": "npm install --prefix frontend",
    "install-backend": "npm install --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "build": "npm run install-backend && npm run build:backend && npm run install-frontend && npm run build:frontend"
  },
  "dependencies": {
    "cloudinary": "^2.10.0"
  }
}